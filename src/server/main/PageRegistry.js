// src/server/90_main/PageRegistry.js

/**
 * PageRegistry
 * Tempat mendaftarkan pemetaan URL parameter (?page=...) ke file HTML.
 */
const PageRegistry = {
  'home': 'src/clients/pages/Home',
  'login': 'src/clients/pages/login/Login',
  // Tambahkan halaman lain di sini
};